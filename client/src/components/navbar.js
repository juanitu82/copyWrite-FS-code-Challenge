import Form from "./form";
import './navBar.css';

const Navbar = () => {
    return(
        <nav className="container-fluid">
            <Form className='form' />
        </nav>

    )
}

export default Navbar

